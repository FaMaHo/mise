import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF1A1A1A);
  static const background = Color(0xFFF8F7F4);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceSecondary = Color(0xFFF1EFE8);

  // Semantic
  static const success = Color(0xFF27500A);
  static const successBg = Color(0xFFEAF3DE);
  static const warning = Color(0xFF633806);
  static const warningBg = Color(0xFFFAEEDA);
  static const danger = Color(0xFF791F1F);
  static const dangerBg = Color(0xFFFCEBEB);
  static const info = Color(0xFF0C447C);
  static const infoBg = Color(0xFFE6F1FB);

  // Text
  static const textPrimary = Color(0xFF1A1A1A);
  static const textSecondary = Color(0xFF5F5E5A);
  static const textTertiary = Color(0xFF888780);

  // Border
  static const border = Color(0x26000000); // 15% black
}