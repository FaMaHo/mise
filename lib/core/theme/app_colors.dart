import 'package:flutter/material.dart';

class AppColors {
  // Base surfaces
  static const background = Color(0xFFF5F5F0);
  static const surface = Color(0xFFEDE8DF);
  static const surfaceSecondary = Color(0xFFE6D8C3);

  // Primary action
  static const primary = Color(0xFF5D866C);
  static const primaryLight = Color(0xFFC8DCCC);
  static const primaryText = Color(0xFF2A4A32);

  // Borders & dividers
  static const border = Color(0xFFDDD0BC);
  static const borderStrong = Color(0xFFC2A68C);

  // Text
  static const textPrimary = Color(0xFF2C2420);
  static const textSecondary = Color(0xFF8A7A6A);
  static const textTertiary = Color(0xFF9A8A78);
  static const textDisabled = Color(0xFFB0A090);

  // Semantic — warning (amber tones)
  static const warningBg = Color(0xFFF0DFB8);
  static const warningText = Color(0xFF6B4A00);

  // Semantic — danger (soft red-brown)
  static const dangerBg = Color(0xFFEDD4C8);
  static const dangerText = Color(0xFF7A2A1A);

  // Semantic — info (tan)
  static const infoBg = Color(0xFFE6D8C3);
  static const infoText = Color(0xFF5A3A20);

  // Semantic — success (sage)
  static const successBg = Color(0xFFC8DCCC);
  static const successText = Color(0xFF2A4A32);

  // Live dot
  static const live = Color(0xFF5D866C);
}
